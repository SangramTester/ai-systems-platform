# AI Systems Platform

Minimal scaffold for the `ai-systems-platform` Python project.

Setup:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Run tests:

```bash
pytest
```
